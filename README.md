# Система мониторинга комментариев

Автоматическая система для мониторинга комментариев из YouTube, VK и Reddit с отправкой уведомлений в Telegram через API.

## Описание

Система позволяет отслеживать новые комментарии из социальных сетей и автоматически отправлять уведомления в Telegram группу с разделением по топикам. Поддерживает получение комментариев и ответов на них.

**Особенности:**
- Параллельная обработка всех парсеров для максимальной скорости
- Автоматические повторы при временных ошибках и таймаутах
- Фильтрация комментариев по времени запуска (не отправляет старые комментарии)
- Сохранение состояния между перезапусками
- Метрики и статистика работы системы

## Установка и настройка

### Шаг 1: Распаковка архива
1. Скачайте архив `CommentParser.zip` https://github.com/ohneRE-L/CommentParser/releases
2. Распакуйте в любую папку (например, `C:\CommentParser\` или `/home/user/CommentParser/`)
3. Откройте папку в проводнике

### Шаг 2: Установка Python

**Windows:**
1. Скачайте Python с https://python.org
2. При установке поставьте галочку "Add Python to PATH"
3. Перезапустите командную строку

**Linux/Ubuntu:**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

**CentOS/RHEL:**
```bash
sudo yum install python3 python3-pip
```

### Шаг 3: Установка зависимостей
1. Откройте командную строку/терминал в папке с проектом
2. Выполните команду:
```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка API ключей

**Важно:** Файл `config.txt` с реальными ключами не должен попадать в репозиторий (добавлен в `.gitignore`).

**Способ 1: Использование config.txt (рекомендуется для локальной разработки)**
1. Скопируйте шаблон: `cp config.txt.example config.txt` (Linux/Mac) или скопируйте файл вручную (Windows)
2. Откройте файл `config.txt` в любом текстовом редакторе
3. Замените все `YOUR_XXX_HERE` на ваши реальные ключи:

**Способ 2: Использование переменных окружения (рекомендуется для продакшена)**
Установите переменные окружения перед запуском:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен"
export TELEGRAM_GROUP_ID="ваш_id"
# и т.д. для всех необходимых ключей
```

**Приоритет:** Переменные окружения > config.txt

**Необходимые ключи:**

**Telegram (обязательно):**
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `TELEGRAM_GROUP_ID` - ID группы (начинается с -100)
- `TELEGRAM_TOPIC_YOUTUBE` - ID топика для YouTube
- `TELEGRAM_TOPIC_VK` - ID топика для VK
- `TELEGRAM_TOPIC_REDDIT` - ID топика для Reddit

**YouTube (опционально):**
- `YOUTUBE_API_KEY` - ключ из Google Cloud Console
- `YOUTUBE_CHANNEL_ID` - ID канала

**VK (опционально):**
- `VK_ACCESS_TOKEN` - токен из VK for Developers
- `VK_GROUP_ID` - ID группы
- `VK_GROUP_URL` - URL группы (например, `https://vk.com/club123456789`)

**Reddit (опционально):**
- `REDDIT_CLIENT_ID` - ID приложения из Reddit
- `REDDIT_CLIENT_SECRET` - секрет приложения
- `REDDIT_USER_AGENT` - User Agent (например, "MyApp/1.0")
- `REDDIT_SUBREDDITS` - список сабреддитов через запятую (например, "python,programming,javascript")

### Шаг 5: Запуск системы
```bash
python main.py
```

**Проверка работы:**
- Должно появиться "Telegram бот подключен успешно"
- В логах не должно быть критических ошибок
- Комментарии будут приходить в Telegram группу
- Каждые 10 проверок будет выводиться статистика

## Получение API ключей

### YouTube API

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите YouTube Data API v3
4. Создайте API ключ в разделе "Credentials"
5. Получите ID канала из URL канала (например, из `https://www.youtube.com/channel/UCxxxxxxxxxxxxx`)

### VK API

1. Перейдите в [VK для разработчиков](https://dev.vk.com/ru/admin/apps-list)
2. Создайте новое приложение
3. В типе приложения выбираете: "Плагин для сообществ"
4. Получите сервисный ключ в разделе "Разработка -> Ключи доступа"
5. Получите ID группы из URL группы (например, из `https://vk.com/club123456789`)

### Reddit API

1. Перейдите в [Reddit App Preferences](https://www.reddit.com/prefs/apps)
2. Нажмите "Create App" или "Create Another App"
3. Выберите тип "script" для личного использования
4. Получите client_id (Находится под строкой "personal use script") и client_secret
5. Укажите user_agent (например, "MyApp/1.0")
6. Укажите список сабреддитов через запятую (например, "python,programming,javascript")

### Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте бота в группу и получите ID группы
4. Настройте топики в группе для разделения уведомлений
5. Получите ID топиков из ссылок на сообщения в топиках

## Архитектура системы

```
├── main.py                    # Основной файл с мониторингом
├── vk_parser.py              # VK парсер через API
├── youtube_parser.py         # YouTube парсер через API
├── reddit_parser.py          # Reddit парсер через API
├── config.txt                # Конфигурация (не в репозитории, создайте из config.txt.example)
├── config.txt.example        # Шаблон конфигурации
├── .gitignore                # Исключения для git
├── requirements.txt           # Зависимости
├── monitor_state_improved.json # Состояние системы
├── comments_monitor_improved.log # Логи
└── README.md                 # Документация
```

## Настройки

### Режимы работы

- Автоопределение окружения (локальный/сервер) с интервалом проверки: 30 секунд

### Параметры парсеров

- **Reddit**: до 20 комментариев с каждого из 20 последних постов каждого сабреддита
- **VK и YouTube**: до 30 комментариев с каждого из 20 последних постов/видео
- Комментарии, написанные до запуска парсера, не отправляются
- Максимум 10 новых комментариев отправляется в Telegram за одну проверку

## Возможности

- **YouTube**: мониторинг комментариев и ответов на них
- **VK**: мониторинг всех комментариев через API
- **Reddit**: мониторинг комментариев из сабреддитов (настраиваемое количество)
- **Telegram**: уведомления в топики с разделением по источникам
- **Фильтрация**: избежание дублирования комментариев (по тексту и timestamp)
- **Состояние**: сохранение состояния между запусками
- **Логирование**: подробные логи работы системы
- **Метрики**: автоматический вывод статистики каждые 10 проверок
- **Retry логика**: автоматические повторы при временных ошибках (до 3 попыток)

## Метрики и статистика

Каждые 10 проверок система автоматически выводит статистику:

```
==================================================
СТАТИСТИКА МОНИТОРИНГА
Время работы: 0:10:00
Всего проверок: 20
Всего комментариев найдено: 150
Всего комментариев отправлено: 45
Всего ошибок: 0
--------------------------------------------------
VK: проверок=20, найдено=80, ошибок=0
YouTube: проверок=20, найдено=50, ошибок=0
Reddit (r/python): проверок=20, найдено=20, ошибок=0
==================================================
```

## Логирование

Логи сохраняются в файл `comments_monitor_improved.log` и выводятся в консоль. Уровень логирования настраивается в коде.

## Устранение неполадок

### VK API ошибки

- Проверьте правильность access token
- Проверьте ID группы
- Система автоматически повторяет запросы при временных ошибках

### YouTube API ошибки

- Проверьте правильность API ключа
- Убедитесь, что YouTube Data API v3 включен
- Проверьте ID канала
- Сообщения "Таймаут запроса, повтор через Xс" - это нормально, система автоматически повторит запрос

### Reddit API ошибки

- Проверьте правильность client_id и client_secret
- Проверьте user_agent
- Таймауты при большом количестве параллельных запросов - нормально, система автоматически повторит запросы (до 3 раз)

### Telegram ошибки

- Проверьте токен бота
- Убедитесь, что бот добавлен в группу
- Проверьте ID группы и топиков
- Убедитесь, что у бота есть права на отправку сообщений
- Система автоматически повторяет отправку при таймаутах (до 3 раз)

### Общие проблемы

- Система не запустится без подключения к Telegram боту
- Проверьте интернет-соединение
- Убедитесь, что все API ключи действительны
- Система проверяет конфигурацию при запуске

### Ошибка "Module not found":
```bash
pip install -r requirements.txt
```

### Ошибка API ключей:
- Проверьте правильность ключей в `config.txt`
- Убедитесь, что API включены в соответствующих сервисах

### Telegram не работает:
- Проверьте, что бот добавлен в группу
- Убедитесь, что ID группы правильный (начинается с -100)
- Проверьте ID топиков

## Настройка для продакшена

Для серверного режима система автоматически определяет окружение и устанавливает интервал проверки 30 секунд.

**Рекомендации для продакшена:**
- Используйте systemd service для автозапуска
- Настройте ротацию логов
- Мониторьте метрики статистики

## Безопасность

### Защита API ключей

**⚠️ ВАЖНО:** Никогда не публикуйте файл `config.txt` с реальными API ключами!

**Меры защиты:**
1. Файл `config.txt` добавлен в `.gitignore` и не будет загружен в репозиторий
2. Используйте `config.txt.example` как шаблон (без реальных ключей)
3. Ограничьте доступ к файлу: `chmod 600 config.txt` (Linux/Mac)
4. Для продакшена рекомендуется использовать переменные окружения вместо файла

**Если ключи случайно попали в репозиторий:**
1. Немедленно отзовите/пересоздайте все API ключи
2. Удалите файл из истории git: `git rm --cached config.txt`
3. Убедитесь, что `.gitignore` содержит `config.txt`

## Поддержка

При возникновении проблем:

1. Проверьте логи: `tail -f comments_monitor_improved.log` (Linux) или откройте файл в текстовом редакторе (Windows)
2. Убедитесь, что все API ключи настроены
3. Проверьте интернет соединение
4. Посмотрите статистику системы (выводится каждые 10 проверок)
5. Перезапустите систему

**Система готова к работе 24/7!** 🎯
